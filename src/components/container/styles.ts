import { css } from 'lit';

export const styles = css`
  :host {
    display: block;
  }

  .container {
    max-width: var(--finap-container-max-width);
    margin: 0 auto;
    padding: 0 var(--finap-space-5);
  }
`;
